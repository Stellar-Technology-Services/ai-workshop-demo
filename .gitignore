# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/

# IDE / tooling
.vs/
.vscode/*
# ...but track the workshop's MCP + Copilot-approval teaching config (see docs/adr/004)
!.vscode/mcp.json
!.vscode/settings.json
*.user
*.suo
*.userprefs

# Claude Code per-machine overrides (never committed)
.claude/settings.local.json

# .NET
*.nupkg
project.lock.json

# Local SQLite database (created on first run; never committed)
*.db
*.db-shm
*.db-wal

# Secrets / local-only config (never committed) — see SPEC §6
appsettings.Development.json

# OS cruft
.DS_Store
Thumbs.db
.env