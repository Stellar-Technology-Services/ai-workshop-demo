@page "/"

<PageTitle>ClaimsChat</PageTitle>

<h1>ClaimsChat</h1>

<p>
    A small demo app for chatting with claims documents. This is the workshop's
    teaching vehicle — explore the <a href="documents">Documents</a> and
    <a href="chat">Chat</a> pages.
</p>

<p class="muted">
    Skeleton (ticket T1): the app runs end-to-end with a stub chat model, so no
    AI key is required yet. Preset documents, lexical retrieval, and the real
    Azure AI Foundry model arrive in later tickets.
</p>
